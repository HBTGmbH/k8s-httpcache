{{- /*
Render arbitrary user-supplied manifests alongside the release. Each entry may be
a YAML/JSON object or a (templated) string. Both are passed through the render
helper so they can reference release values using either << ... >> or {{ ... }}
delimiters, e.g. name: << include "k8s-httpcache.fullname" . >>-extra.
*/}}
{{- range .Values.extraManifests }}
---
{{ include "k8s-httpcache.render" (dict "value" . "ctx" $) }}
{{- end }}
